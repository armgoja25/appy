<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stretching Guide</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #FFF0F5;
            color: #4B0082;
            text-align: center;
        }
        .container {
            width: 60%;
            margin: auto;
            background: #FFFFF0;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px #888;
        }
        button {
            background-color: #FF69B4;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
        }
        button:hover {
            background-color: #FF1493;
        }
    </style>
    <script>
        function showPage(pageId) {
            document.querySelectorAll('.page').forEach(page => page.style.display = 'none');
            document.getElementById(pageId).style.display = 'block';
        }

        function updateStretchingGuide() {
            const muscle = document.getElementById('muscle').value;
            let content = "";
            let guidance = "";

            if (muscle === "neck") {
                content = `<p><b>ท่าก้ม-เงยศีรษะ:</b> ก้มศีรษะไปด้านหน้าให้คางแตะคอ ค้างไว้ 2-3 วินาที จากนั้นเงยศีรษะไปด้านหลัง</p>
                           <p><b>ท่าหันหน้าไปด้านข้าง:</b> ค่อยๆ หันศีรษะไปทางซ้าย ค้างไว้สองสามวินาที แล้วทำซ้ำกับด้านขวา</p>`;
                guidance = "ทำท่าอย่างช้า ๆ และไม่ออกแรงมากเกินไปเพื่อป้องกันอาการบาดเจ็บบริเวณคอ";
            } else if (muscle === "back") {
                content = `<p><b>ท่าชันเข่า:</b> นอนหงาย ชันเข่าทั้งสองข้าง ใช้มือจับเข่าแล้วค่อยๆ ดึงเข้าหาตัว</p>
                           <p><b>ท่ากดหลัง:</b> เกร็งกล้ามเนื้อหน้าท้องเพื่อกดหลังให้แนบพื้น ค้างไว้ 5 วินาที ทำซ้ำ 5 ครั้ง</p>`;
                guidance = "รักษาแนวกระดูกสันหลังให้ตรงเสมอ หลีกเลี่ยงการบิดหรือเคลื่อนไหวที่รุนแรง";
            } else if (muscle === "legs") {
                content = `<p><b>ท่ายืดเหยียดหน้าขา:</b> พับเข่าด้านที่ต้องการยืดไปด้านหลัง ใช้มือจับขาหรือข้อเท้า ค่อยๆ ดึงเข้าหาสะโพก</p>
                           <p><b>ท่ายืดเหยียดแฮมสตริง:</b> ยืดขาที่ต้องการไปด้านหน้า ก้มตัวลงจนรู้สึกตึงขา</p>`;
                guidance = "อย่ากดน้ำหนักลงที่ขามากเกินไป ค่อย ๆ ยืดออกและหายใจเข้าลึก ๆ";
            } else if (muscle === "arms") {
                content = `<p><b>ท่าหมุนข้อไหล่:</b> ยืนก้มหลังลงเล็กน้อย ปล่อยแขนห้อย แล้วค่อยๆ หมุนแขนเป็นวงกลม</p>
                           <p><b>ท่าไต่กำแพง:</b> ยืนหันหน้าเข้ากำแพง แล้วค่อยๆ เลื่อนมือขึ้นไปจนรู้สึกตึง</p>`;
                guidance = "ระวังอย่าบิดข้อมือหรือไหล่มากเกินไป หยุดหากรู้สึกเจ็บ";
            }

            document.getElementById('stretching-content').innerHTML = content;
            document.getElementById('guidance-content').innerText = guidance;
            showPage('page2');
        }
    </script>
</head>
<body>
    <div class="container">
        <div id="page1" class="page">
            <h2>ข้อมูลส่วนตัว</h2>
            <label>ชื่อ: <input type="text" id="name"></label><br>
            <label>เลือกกล้ามเนื้อ:
                <select id="muscle">
                    <option value="neck">คอ</option>
                    <option value="back">หลัง</option>
                    <option value="legs">ขา</option>
                    <option value="arms">แขน</option>
                </select>
            </label><br>
            <label>ระดับความเจ็บ (1-10): <input type="number" id="pain" min="1" max="10"></label><br>
            <button onclick="updateStretchingGuide()">ถัดไป</button>
        </div>

        <div id="page2" class="page" style="display: none;">
            <h2>ท่ายืดกล้ามเนื้อตามที่เลือก</h2>
            <div id="stretching-content"></div>
            <button onclick="showPage('page3')">ถัดไป</button>
        </div>

        <div id="page3" class="page" style="display: none;">
            <h2>คำแนะนำในการปฏิบัติ</h2>
            <p id="guidance-content"></p>
            <button onclick="showPage('page4')">ถัดไป</button>
        </div>

        <div id="page4" class="page" style="display: none;">
            <h2>WELLNESSARC</h2>
            <p>ขอบคุณที่ใช้ WellnessArc หวังว่าคุณจะได้รับประโยชน์จากการยืดกล้ามเนื้อในวันนี้</p>
            <button onclick="showPage('page1')">เริ่มใหม่</button>
        </div>
    </div>
</body>
</html>
